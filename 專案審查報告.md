# Star Citizen 工具網站 — 全專案審查報告

審查日期：2026-08-21
審查範圍：134 個程式檔（`app/` `src/` `tasks/` `bot/` `frontend/src/` `conf/` `docker/` `tests/`），四個維度並行掃描：程式碼品質、效能、介面一致性、安全性。

> **重要**：所有結論都經過實際讀碼驗證。附錄列出 9 項「聽起來嚴重但查證後為誤報」的項目，其中 5 項是暫存檔案不完整造成的假警報 —— **你的專案沒有那些問題**，不要浪費時間去修。

---

## 先看這裡：最該立刻處理的 5 件事

| # | 問題 | 誰能利用 | 修復時間 |
|---|------|---------|---------|
| **1** | `.env` 從未改過，`ADMIN_PASSWORD=admin` | **任何未登入的人** | 5 分鐘 |
| **2** | Mongo / MySQL / Redis 三個 port 對外開放，Mongo 完全無認證 | **任何未登入的人**（若主機有公網 IP） | 5 分鐘 |
| **3** | 10 支後台路由缺角色控管，玩家帳號可改／刪任何玩家資料 | 任何註冊過的玩家 | 30 分鐘 |
| **4** | 部分更新會清空欄位，可讓任何玩家帳號**永久無法登入** | 任何註冊過的玩家 | 20 分鐘 |
| **5** | 同步沒有鎖，併發執行會把物品主檔大面積標成「已下架」 | 到期就會自己發生 | 30 分鐘 |

第 1、2 項是設定問題不用改程式。第 3、4、5 項是這次審查找到的三個真實缺陷。

---

# A. 安全性

### A1. 🔴 `.env` 與 `.env.default` 位元完全相同 — 未登入者可用 admin / admin 進後台

**位置**：`.env`（與 `.env.default` diff 結果為空）、`run.py:41-48`

`run.py:44` 在資料庫沒有 admin 帳號時會用 `ADMIN_PASSWORD` 建立帳號，而該值目前是範本值 `admin`。README 自己註明範本值是公開的。任何人對站台打一次 `POST /auth/login {"username":"admin","password":"admin"}` 就取得 admin token，可讀寫全部使用者、玩家名冊、庫存、稽核日誌。

同一份檔案還帶著範本值的 `MYSQL_ROOT_PASSWORD`、`MYSQL_PASSWORD`、`REDIS_PASSWORD` —— 配合 A2 就是完整的資料庫接管。

**修復**
1. 登入後台改掉 admin 密碼
2. `.env` 的四個密碼欄位換成 `openssl rand -base64 24` 產物，重建 db 容器
3. `run.py` 加保護：`ADMIN_PASSWORD` 未設或等於 `admin` 時直接 `raise SystemExit` 拒絕啟動，不要只印警告

**工作量**：小

---

### A2. 🔴 三個資料庫 port 綁在 0.0.0.0，且 README 說明與實作相反

**位置**：`docker-compose.db.yml:6, 21, 42`、`conf/config.ini:23`、`README.md:152-155`

```yaml
ports:
  - "${MONGO_PORT:-27017}:27017"   # 沒有 127.0.0.1 前綴 → 綁 0.0.0.0
```

README 的 Port 對照表明確寫「三個資料庫都沒有對主機映射 port，要連進去一律走 `docker compose exec`」，但實際三個都發佈了。Docker 會直接寫 iptables **繞過 ufw**。而 `MONGO_URI=mongodb://mongo:27017` 沒有任何帳密，Mongo 也沒設 `--auth`。

若主機有公網 IP 且無雲端安全群組阻擋，任何人 `mongosh mongodb://<主機IP>:27017` 就能讀寫整個資料庫（含 `users` 的密碼 hash、`players`、`inventory`），並直接把自己的 `role` 改成 admin。

**修復**：三處改成 `"127.0.0.1:${MONGO_PORT:-27017}:27017"`（僅本機可連），或整段移除、需要時用 `docker compose exec`。同時為 Mongo 開 `--auth` 並在 `MONGO_URI` 帶帳密。改完把 README 與實作對齊。

**工作量**：小

---

### A3. 🔴 10 支後台路由只有 `@jwt_required()`，沒有角色檢查

**位置**：`app/player/view.py:69, 76, 86, 106, 126`、`app/blueprint/view.py:26, 34, 43, 55, 69`、`app/log/view.py:8`

已用程式碼掃描逐條確認。兩套身分體系共用同一把金鑰（`conf/config.py:12-13` 把 `SECRET_KEY` 同時指派給 `JWT_SECRET_KEY`），只有一個 `JWTManager`。`@jwt_required()` 只驗簽章，**不看 identity 格式、也不看自訂的 `type: 'player'` claim** —— 全專案唯一讀那個 claim 的地方是 `player_required`，後台路由上沒有任何地方驗證它。

`require_role` 之所以擋得住玩家 token 純屬巧合：identity 是 `player:<scid>`，`User.find_by_username('player:Tom')` 查不到 → 403。所以**有** `require_role` 的路由安全，**沒有**的全開。

以下路由「viewer 帳號」與「玩家 token」都能打：

| Method | Path | 後果 |
|---|---|---|
| GET | `/player/` `/player/<id>` | 全公會 PII：本名、SCID、暱稱、Discord 帳號與 ID、備註 |
| POST | `/player/` | 憑空造出玩家紀錄 |
| PUT | `/player/<id>` | 改任何人的資料（含清空，見 B1） |
| DELETE | `/player/<id>` | 軟刪任何玩家，讓對方**無法再登入** |
| GET/POST/PUT | `/blueprint/*` | 讀／造／改任何人的藍圖 |
| DELETE | `/blueprint/<id>` | **繞過**自助端的歸屬檢查刪別人的藍圖 |
| GET | `/log/` | 完整後台稽核日誌：管理帳號名稱、登入成功/失敗紀錄、庫存操作明細 |

好消息：`Player._serialize` 預設會 pop 掉 `password`，**bcrypt hash 沒有外洩**。

**修復**：GET 加 `@require_role('admin','operator','viewer')`，POST/PUT/DELETE 加 `@require_role('admin','operator')`。**更根本的修法**：在 `src/permissions.py` 新增 `admin_required` 裝飾器（內含「`get_jwt().get('type') == 'player'` → 403」+ 原本的 role 檢查），套到所有後台路由，不要繼續依賴「前綴讓查詢找不到人」這種間接效果。

**工作量**：小（純加裝飾器）～中（含 `admin_required` 抽象）

---

### A4. 🟡 `/inventory/` 讀取端點可查別人的個人庫

**位置**：`app/inventory/view.py:48-55`（`_owner_from_args`）、`:94, :183, :235`

`_owner_from_args` 直接吃 query string 的 `owner_type` 與 `player`，完全不比對呼叫者身分。玩家 token 打 `GET /inventory/?owner_type=player&player=<別人的handle>` 就拿到對方個人庫全部品項、位置、容器；`/inventory/history` 更是全域異動紀錄（含操作者與備註自由文字）。

**設計脈絡要注意**：`MyPlayerView.vue` 刻意用玩家 token 呼叫 `/inventory/locations`、`/item/search`、`/inventory/where/<id>`，所以**不能**一刀切斷玩家對 `/inventory/*` 的存取。`/inventory/where/<item_id>`（回傳「公會裡誰有這個東西」）屬刻意公開的功能需求，不算漏洞，但值得跟公會確認這是共識。

**修復**：只針對 `list_stock` / `capacity` / `history` 三支加保護 —— 若 `get_jwt().get('type') == 'player'`，強制 `owner_type='player'` 且 `player` = 自己的 SCID；`/inventory/history` 對玩家 token 回 403（自助端已有 `/player/inventory/history`）。`/locations`、`/where`、`/item/*` 保持現狀。

**工作量**：小

---

### A5. 🟡 Discord bot 的公會共享庫沒有任何寫入限制

**位置**：`.env.default:40`（`WMS_OPERATOR_ROLE=` 空值）、`src/__init__.py:85`、`bot/cogs/stock.py:43`

註解自己寫了「環境變數留空 = 所有人都能動」。`.env` 目前是空的，所以 Discord 伺服器裡的任何成員都能用斜線指令搬空公會共享庫。Web 端的 `/inventory/add|remove|move` 有 `require_role('admin','operator')` 保護，bot 端等於一個沒鎖的後門。

**修復**：`.env` 填入實際的 Discord 角色名稱；不用 bot 就把 `DISCORD_TOKEN` 留空。並把「留空 = 全開」改成「留空 = 全鎖」的 fail-closed 預設。

**工作量**：小

---

### A6. 🟡 完全沒有 token 撤銷機制

**位置**：`src/models/user.py:93-110`、`app/player/view.py:205-216`、`src/__init__.py:37-38`（8 小時 / 30 天）

沒有 JWT blocklist、沒有 token version。

- **被開除的後台帳號**：`require_role` 會即時擋，但所有「只有 `@jwt_required()`」的路由在 access token 到期前的 **8 小時內照常可用** —— 配合 A3，離職者 8 小時內還能刪玩家、刪藍圖。
- **被軟刪的玩家**：`/player/refresh` 只檢查 identity 前綴、不查資料庫，能用 refresh token **持續 30 天換發新 token**；`/player/inventory/*` 全都只用 identity 字串當 key、不查名冊，所以軟刪後個人庫寫入權限完整保留。

**修復**：最低成本做法 ——（a）`/player/refresh` 加一行存在性檢查；（b）`player_required` 裡驗證玩家文件仍存在且 `deleted_at is None`，結果放進 `g` 順便消除 `_self_player_doc()` 的重複查詢。要真撤銷則用 Redis 存 `jti` 黑名單。

**工作量**：小（a+b）／中（含 blocklist）

---

### A7. 🟡 Rate limit 的 key 是 nginx 容器的 IP，全站共用一個計數桶

**位置**：`src/limiter.py:11-15`、`app/__init__.py:58`（全庫查無 `ProxyFix`）

nginx 有設 `X-Real-IP` / `X-Forwarded-For`，但 Flask 沒掛 `ProxyFix`，所以 `get_remote_address()` 回傳的是 nginx 容器 IP —— 對所有外部使用者都是同一個值。後果雙向都壞：

- `10 per minute` 變成**全站每分鐘 10 次登入**。一個人連打幾次，整個公會都登不進來。
- 想針對單一攻擊 IP 限流則完全無效。

另外 `default_limits=[]`，所以只有 5 支路由有限流。`/item/sync`（觸發約 130 次外部 API 呼叫、5–10 分鐘工作）與 `/item/search`（全表掃描，見 D1）都沒有。

**修復**：`app/__init__.py` 加 `app.wsgi_app = ProxyFix(app.wsgi_app, x_for=1, x_proto=1, x_host=1)`。登入收到 `5 per minute` + `50 per hour`。`/item/sync` 加 `2 per hour`，`/item/search` 加 `60 per minute`。

**工作量**：小

---

### A8. 🟡 `/apidocs` 對外公開，列舉所有 endpoint

**位置**：`app/__init__.py:56`（`Swagger(app, ...)` 無 `decorators`）

未登入者可瀏覽完整 API 清單，含 `/user/`、`/item/sync`、`/inventory/move` 的參數 schema。各路由仍需 token，所以不是直接漏洞，但把 A3 的攻擊面攤開給人看。順帶：`app/__init__.py:29-37` 的 Swagger template 還留著樣板資訊 —— `"title": "My API"`、`"description": "練習用 API文檔"`、`open222333@gmail.com`、`www.test.com`，會隨 `/apidocs` 一起公開。

**修復**：`Swagger(app, template=template, decorators=[jwt_required(), require_role('admin')])`，或只在開發環境註冊。同時填入真實專案資訊。

**工作量**：小

---

# B. 資料正確性

### B1. 🔴 部分更新會把沒傳的欄位清成空字串 —— 含 `star_citizen_id`

**位置**：`app/player/view.py:54-62`（`_serialize_form`）、`:110-117`、`src/models/player.py:100-113`、同一個 bug 在 `app/blueprint/view.py:15-23, 55-66`

`_serialize_form` **無條件回傳全部 6 個 key**，缺的填 `''`；而 `Player.update` 的過濾條件是 `if v is not None` —— **擋 `None` 但不擋 `''`**，所以空字串會被 `$set` 進資料庫。`:113` 那道守衛寫的是 `if 'star_citizen_id' in data and not fields[...]`，**沒傳這個 key 就完全繞過**。

後果：送 `PUT /player/<受害者id> {"notes":"x"}` 會把受害者的本名、SCID、暱稱、Discord 資料全部清成 `''`。SCID 被清空後：

- 對方**永遠無法登入**（空值直接 401）
- 個人庫存的 `inventory.player` 成為孤兒
- 因為 unique index 不是 partial，**第二個被清空的玩家會撞 DuplicateKeyError**，留下不一致狀態

配合 A3（玩家 token 可打這支路由），這是一個可被任何玩家觸發的**永久帳號封鎖武器**。後台 UI 剛好都送完整表單所以不會誤觸 —— 這是純攻擊向量，不是偶發資料遺失。

`app/blueprint/view.py` 是同一個 bug 的複製：只帶 `{"notes":"x"}` 會把 `name` 清空、`player_id` 變 `None`（藍圖脫離擁有者）、`unlock_status` 靜默重設回 `'obtained'`（把 `unlocked` 狀態吃掉）。

**修復**：`_serialize_form` 改成只回傳 `data` 裡實際出現的 key（`{k: (data.get(k) or '').strip() for k in FIELDS if k in data}`）。同一檔案的 `update_current_player`（`:261-270`）已經是正確寫法，照抄即可。model 層的過濾改用明確 sentinel（`src/models/user.py:8` 的 `_UNSET` 是好範例），不要用 `''`/`None` 的真假值當語意。

**工作量**：小

---

### B2. 🔴 同步沒有鎖，併發執行會互相把主檔標成「已下架」

**位置**：`tasks/scdata_sync.py:206-240`（特別是 `:224-225` 與 `:236`）、`tasks/celeryconfig.py:25-28`、`docker-compose.worker.yml:8`（`--concurrency=2`）

心跳每 5 分鐘判斷一次是否該同步，依據是 `sync_runs` 最後一筆成功紀錄的 `finished_at`。但那筆紀錄是在同步**全部跑完之後**才寫入，而全量同步要 5–10 分鐘（程式自己的註解寫的）。註解宣稱「直接呼叫而不用 `.delay()` 可以避免重複排程」，**實際完全沒防到** —— 沒有任何鎖或 in-progress 標記：

```
T+0   心跳 1 → due → 同步 A 開始（佔 worker slot 1，跑 5-10 分鐘）
T+5   心跳 2 → sync_runs 還沒有新紀錄 → 依然 due → 同步 B 開始（佔 slot 2）
```

更嚴重的是 `_sync_wiki_resource` 結尾的下架步驟（`:91-94`）：

```python
update_many({'_sync.run_id': {'$ne': run_id}, 'is_current': {'$ne': False}},
            {'$set': {'is_current': False, 'retired_at': stamp}})
```

A 和 B 的 `run_id` 不同。B 的下架步驟若在 A 還在寫入時執行，**所有帶 A 的 run_id 的文件會被標成 `is_current=False`**。而所有查詢都過濾 `is_current: True` —— 結果是物品搜尋、autocomplete、庫存 join **大面積變空**，直到下次同步才修正。

這已經越過效能範疇變成資料正確性問題。

**修復**：Redis 已經在了（`src/redis_client.py`），加分散式鎖是最小改動：

```python
if not get_redis().set('scdata_sync:lock', run_id, nx=True, ex=3600):
    return {'skipped': True, 'reason': 'already_running'}
try:
    ...
finally:
    get_redis().delete('scdata_sync:lock')
```

**工作量**：小

---

### B3. 🟡 同步一失敗就每 5 分鐘無限重跑

**位置**：`tasks/scdata_sync.py:224-226, 161-164, 195`

`sync_scdata` 對單一資源的失敗做了「不拖垮其他資源」的處理，代價是只要任何一個 resource 或任何一個 UEX endpoint 失敗，`errors` 就非空 → `ok: False`。而心跳只找 `find_one({'ok': True})`，等於這次跑過完全不算，5 分鐘後仍為 due，再跑一次 130 次外部請求的全量同步 —— 直到某次完全乾淨為止。

社群 API 常態性不穩，所以這會變成永久性的每 5 分鐘轟炸，並與 B2 的併發問題疊加。

**修復**：`is_due()` 的基準改成「最後一次**嘗試**的 `finished_at`」（`find_one({}, sort=...)`），用 `ok` 決定是否走較長的 backoff（例如失敗後 30 分鐘再試），並限制連續失敗次數。

**工作量**：小

---

### B4. 🟡 cron 時區前後不一致，實際觸發時間差 8 小時

**位置**：`src/models/sync_schedule.py:111, 116`、`tasks/celeryconfig.py:3`、`frontend/src/views/SettingsView.vue:48`

`celeryconfig.py` 設定 `timezone = 'Asia/Taipei'`，設定頁的說明文字也直接告訴使用者「`30 4 * * 1` = 每週一 04:30」。但 `SyncSchedule.is_due()` 用的是 `datetime.utcnow()` + croniter，資料庫裡的 cron 字串是**用 UTC 解讀的** —— 實際會在台北時間 12:30 觸發。程式註解還寫「跟原本 celeryconfig.py 的排程一致」，搬到資料庫的過程中靜默位移了 8 小時。

（這是我上一輪加的功能，我的疏漏。）

**修復**：`is_due()` 改用 `zoneinfo.ZoneInfo('Asia/Taipei')` 做 aware 比較，或把時區存進 `sync_schedule` 文件變成可設定欄位。無論哪個，設定頁的說明文字都要標明時區。

**工作量**：小

---

### B5. 🟡 `self.retry()` 在心跳路徑上失效

**位置**：`tasks/scdata_sync.py:180-183, 236`

`raise self.retry(exc=exc, countdown=600)` 只在透過 `.delay()` 派送時成立。但心跳是**直接函式呼叫**，Celery 的 `Task.retry` 在 `called_directly` 為 True 時會直接重拋原始例外而不排重試 —— 600 秒的 backoff 完全不生效，5 分鐘後心跳又跑一次全量同步。

**修復**：把核心邏輯抽成純函式 `_do_sync(resources, with_uex)`，讓 `sync_scdata`（負責 retry 語意）與 `check_and_run_scheduled_sync`（負責自己的 backoff）各包一層。

**工作量**：小

---

### B6. 🟡 軟刪玩家的 SCID 永久卡死，且沒有還原路徑

**位置**：`src/mongo.py:66`、`src/models/player.py:57, 116-125`、`app/player/view.py:157-168`

`create_index('star_citizen_id', unique=True)` **不是 partial index**，軟刪的文件仍佔用鍵值。所以軟刪玩家「Tom」之後：註冊前的預檢查（會過濾 `deleted_at`）回 `None` → 檢查通過 → `Player.create` 撞 unique index → 409 顯示「遊戲ID『Tom』已經被註冊過了」。

使用者看到「已被註冊」，但後台名冊上看不到這個人，**沒有人能解釋也沒有人能修** —— 全專案沒有任何 un-delete 路由。配合 A3（玩家 token 可任意軟刪別人）就成了永久封鎖武器。

**修復**：索引改成 `create_index('star_citizen_id', unique=True, partialFilterExpression={'deleted_at': None})`（需先 drop 舊索引），並加一支 `POST /player/<id>/restore`。

**工作量**：小

---

### B7. ⚪ 未防護的 `ObjectId()` 讓錯誤 ID 變成 500

**位置**：`src/models/blueprint.py:66, 82, 98`、`src/models/player.py:108`、`src/models/user.py:90`、`src/models/user_template.py:69`

`PUT /player/abc`、`DELETE /blueprint/abc` 等會丟 `bson.errors.InvalidId` → 落到全域 handler → 500 + 記一筆假的 unhandled error。不洩漏資訊、無法放大，但把使用者輸入錯誤誤報成伺服器故障，並在 log 裡製造噪音掩蓋真正的異常。

**修復**：`find_by_id` 已經有正確寫法，把 update / delete / create 路徑補齊即可（抽一個共用的 `_oid(value)`）。

**工作量**：小

---

# C. 前端與使用體驗

## C0. 設計方向建議（先看這個，它決定下面哪些要個別修）

**先修正一個假設**：科幻主題**沒有**漏進後台。`scifi-theme.css` 的 166 行裡，未加命名空間的只有 `:root` 的 `--sf-*` 變數（與後台 `--sb-*`/`--nb-*` 完全不相交）和 `.scifi-page`/`.scifi-card`/`.btn-scifi` 這幾個自訂 class 名，其餘 12 組全部已用 `.scifi-page` 前綴限定。作者已經做對了。

**真正的洩漏是反方向的：Bootstrap 的淺色預設漏進科幻頁。** 科幻主題是一層**不完整**的覆寫 —— 它覆寫了 `.form-control` / `.table` / `.nav-tabs`，卻沒覆寫 `.text-muted` / `.text-success` / `.text-danger` / `.btn-link` / `.form-check-input` / `.alert-*`，而這些在玩家端被大量使用。實算對比：

```
.text-muted rgba(33,37,41,.75) 疊在 --sf-panel #121a2b = 1.09:1   ← 幾乎黑底黑字
.text-success #198754  on #121a2b = 3.84:1   AA 內文 FAIL
.text-danger  #dc3545  on #121a2b = 3.84:1   AA 內文 FAIL
.btn-link     #0d6efd  on #121a2b = 3.86:1   AA 內文 FAIL
```

科幻調色盤**自身**設計良好，全部通過 AA（`--sf-text` 13.77:1、`--sf-accent` 6.71:1、`--sf-accent-2` 7.41:1）。問題不是選錯顏色，是覆寫層漏了一半。

**建議做法**：Bootstrap 5.3.3 已內建這套機制，但本專案**從未設定過 `data-bs-theme`**。具體三步：

1. 四個 `.scifi-page` 根元素加 `data-bs-theme="dark"`
2. `.scifi-page` block 內把 `--sf-*` 映射到 Bootstrap token：
   ```css
   .scifi-page {
     --bs-body-bg: var(--sf-bg-1);        --bs-body-color: var(--sf-text);
     --bs-secondary-color: var(--sf-text-muted);
     --bs-card-bg: var(--sf-panel);       --bs-border-color: var(--sf-border);
     --bs-link-color: var(--sf-accent);
   }
   ```
3. `stores/theme.js` 的 `applyVars()` 加一行 `document.documentElement.setAttribute('data-bs-theme', isDark() ? 'dark' : 'light')`

**這一個 PR 同時修掉 C1、C4、C7、C11、C12 五項**（全部是同一根因），並讓後台深色模式第一次真的能用。約 1–1.5 天。

兩套設計語言**建議刻意保留** —— 後台是資料密集的表格工作台（淺色、高密度），玩家端是低頻的儀式性介面（深色、科幻）。用同一組 token（間距階、圓角、字級、狀態色語意）讓它們是「同一產品的兩個模式」。全部統一到科幻主題要 3–5 天，且深色 HUD 不適合長時間閱讀 8 欄庫存表，還會讓已經寫完的 `stores/theme.js`（175 行、5 種主題色 + 自訂色 + 完整設定 UI）整套報廢。

---

### C1. 🔴 玩家端 22 處說明文字對比 1.09:1，實質看不見

**位置**：`frontend/src/views/MyPlayerView.vue:24, 61, 87, 203-269, 315-325`（22 處 `text-muted`）

使用者實際看到的是黑底黑字：「載入中…」「目前沒有登記任何物品。」「目前沒有人持有這個物品。」「沒有符合的地點，直接輸入即可新增」。庫存紀錄表的**時間欄與備註欄整欄消失**。空狀態訊息全部隱形，玩家會以為頁面壞了。

注意 Bootstrap 用了 `!important`，一般權重覆寫無效。

**修復**：C0 的 token 層（3 行）。或最低限度 `.scifi-page .text-muted { color: var(--sf-text-muted) !important }`。

**工作量**：小

---

### C2. 🔴 玩家端每一個主要按鈕完全沒有鍵盤 focus 指示

**位置**：`frontend/src/assets/scifi-theme.css:114-143`

已用 Bootstrap 原始碼驗證完整繼承鏈：`.btn:focus-visible` 設 `outline: 0` 並改用 `box-shadow: var(--bs-btn-focus-box-shadow)`，而該值依賴 `--bs-btn-focus-shadow-rgb` —— 那個變數**只由 `.btn-primary` 等 variant class 定義**，base `.btn` 沒有。`.btn-scifi` 不是任何 Bootstrap variant，所以 `rgba()` 在計算時失效 → `box-shadow` 退回 `none`，而 `outline: 0` 照樣生效。**結果是零 focus 指示**。

同一條規則的 `color: var(--bs-btn-hover-color)` 也未定義 → 繼承成 `--sf-text` 近白色，疊在漸層亮端 #3ea6ff 上是 **2.05:1** —— focus 狀態比平常更難讀。

影響範圍是玩家端**每一個** CTA 加上後台登入鈕。鍵盤使用者按 Tab 完全不知道焦點在哪。

**修復**：`.btn-scifi` 補 `--bs-btn-focus-shadow-rgb` 與 `--bs-btn-hover-color`，或直接寫 `&:focus-visible { outline: 2px solid var(--sf-accent); outline-offset: 2px }`。

**工作量**：小

---

### C3. 🔴 玩家端表格在手機上撐爆版面

**位置**：`frontend/src/views/MyPlayerView.vue:209, 234, 255, 317`（4 個 table，全部沒有包裝）

後台 7 處表格都包了 `<div style="overflow-x:auto">`，玩家端這 4 個**完全沒有**。庫存紀錄表有 6 欄（時間／動作／物品／地點／數量／備註），時間欄產生「2026/8/21 14:03:22」、地點欄產生「Port Olisar（奧利薩港）」，min-content 寬度遠超 360px 手機視窗，而祖先只有 `max-width:760px` 的 `.container` → **整份文件橫向捲動**。而 `.scifi-page` 用 `background-attachment: fixed`，橫捲時背景不跟著移動，畫面會撕裂。

公會成員最主要的使用場景就是手機查自己的庫存。

**修復**：4 個表格包 `<div class="table-responsive">`。更好是手機斷點改卡片式列表（每筆一張卡、label:value 直排）。

**工作量**：小（包裝）／中（卡片式）

---

### C4. 🔴 後台「深色模式」是假的 —— 只換側邊欄，內容區永遠淺色

**位置**：`frontend/src/stores/theme.js:68`（dark 分支）vs `:82`（light 分支）

兩個分支的 `--content-bg` 分別是 `#f5f6fa` 與 `#eef0f5` —— **都是淺灰**。而全專案從未設定 `data-bs-theme`，所以 Bootstrap 的 card / table / form-control / 文字顏色永遠停在淺色。

實際結果：管理員在設定頁點「深色」，只有 220px 寬的側邊欄跟頂部 navbar 變深，右側 9 個頁面的所有卡片、表格、文字**完全沒變**。這是設定頁上最顯眼的功能，承諾與實際落差極大。

**修復**：C0 的第 3 步（一行）+ dark 分支的 `--content-bg` 改成真正的深色。Bootstrap 5.3.3 已備好完整的 `[data-bs-theme=dark]` token 集。

**工作量**：小

---

### C5. 🔴 錯誤被渲染成空狀態 —— 後端壞了看起來像「沒有資料」

**位置**：`frontend/src/stores/loot.js:36-44`、`stores/player.js:63-66`、`UsersView.vue:216, 223`、`LogsView.vue:65`、`BlueprintListView.vue:94`、`PlayerDetailView.vue:90, 96`、`InventoryListView.vue:231`

```js
if (res) { const data = await res.json(); items.value = data.data || [] }   // 404 → []
```

只檢查 `res` 是否存在（404 的 Response 是 truthy），**不檢查 `data.success`**。所以任何伺服器端錯誤（500、404、權限不足）都會被渲染成一個看起來正常的空表格，後端回的錯誤訊息被丟掉。這是最容易讓人誤判「資料不見了」的失敗模式。

最明顯的實例是 C6。

**修復**：在 `apiFetch` 統一處理 `!res.ok || !data.success`，回傳帶錯誤訊息的 result。空狀態與錯誤狀態必須是兩種不同畫面。全庫共 41 處 `await res.json()` 各自手寫判斷，可一併收斂。

**工作量**：中

---

### C6. 🟡 `/loot` 後端完全不存在，後台戰利品頁靜默空白

**位置**：`app/__init__.py:91-101`（沒有註冊 `/loot`，`app/` 下也沒有 `loot/` 目錄）、`frontend/src/api/index.js:103-113`、`DashboardLayout.vue:50`

後端註冊的是 `/auth /user /admin /log /device /docs /item /inventory /player /blueprint` —— **沒有 `/loot`**。全庫 grep `loot` 在 `.py` 中只剩 `src/scdata.py:163` 的 `is_lootable` 欄位。但前端側：側邊欄「戰利品」→ 路由 `/loot` → `LootListView.vue` → `lootApi` 的 8 個方法全部指向 `/loot/*`。

配合 C5，管理員看到的是一個看起來正常的空表格，「新增戰利品」填完存檔又靜默失敗。

**這條死路徑還蔓延到還活著的頁面**：`PlayerDetailView.vue:89` 也打 `lootApi.list()`，導致「玩家取得紀錄」表格與 4 個統計方塊中的 3 個永遠是 0。

死碼清單（約 450 行）：`stores/loot.js`、`components/LootFormModal.vue`、`views/LootListView.vue`、`components/RarityTag.vue`、`components/FilterBar.vue`、`api/index.js:103-113`、兩份 vite config 的 `/loot` proxy。

（對照確認：`StatusBadge.vue` 與 `TemplateModal.vue` **不是**死碼，仍被 3 個頁面使用。）

**修復**：一次性刪除上述檔案 + 路由 + 側邊欄連結。`PlayerDetailView.vue` 改接 `/inventory/history?player=...` 與 `/blueprint/?player_id=`（後者我上一輪已經接好了）。

**工作量**：中

---

### C7. 🟡 玩家端刪除藍圖沒有確認、失敗也沒有提示

**位置**：`frontend/src/views/MyPlayerView.vue:327, 693-698`

先修正一個假設：後台三個列表頁**都有**確認（`PlayerListView` / `BlueprintListView` / `LootListView` 全部用 `ConfirmModal`）。真正的例外只有玩家端：

```js
if (!res) return                                    // ← 網路失敗，畫面毫無變化
const data = await res.json().catch(() => null)
if (res.ok && data?.success) loadBlueprints()       // ← 失敗時走到這就結束，無 else
```

玩家在手機上點到「刪除」（`btn-link p-0`，沒有 padding，緊貼備註欄右側）就直接刪掉，無法復原。後端回錯誤則畫面**完全沒反應**，玩家會再點一次。

**修復**：引入既有的 `ConfirmModal.vue`，並在 `else` 補錯誤訊息。

**工作量**：小

---

### C8. 🟡 全站 75 個表單控件只有 2 個有 label 關聯

**位置**：全部 13 個含表單的檔案。唯二正確：`LoginView.vue:27-28`、`SettingsView.vue:66-71`

所有 `<label class="form-label">` 都是 input 的**兄弟節點**、沒有 `for`、input 也沒被包在 label 內 → 完全沒有程式化關聯。實際後果：（1）螢幕閱讀器只報「編輯框」；（2）**點擊文字標籤不會 focus 到欄位** —— 這在手機上尤其痛，因為那些標籤本來就用 `.small`。

最痛的是 `MyPlayerView.vue:42-96` 的多筆存入列，每列 3–4 個未標記欄位重複 N 次。

**修復**：導入 `<FormField label="物品" v-model=…>` 包裝元件（用 `useId()` 自動串 `for`/`id`），一次替換 75 處。同時解決 C10 的 modal 樣板重複。

**工作量**：中

---

### C9. 🟡 5 個自訂 autocomplete 下拉完全無法用鍵盤操作

**位置**：`frontend/src/views/MyPlayerView.vue:55-63, 80-90, 128-136, 153-163, 193-200`

五處都是手寫的 `<ul><li @click>` 或 `<li @mousedown.prevent>`。`<li>` 沒有 `tabindex`、沒有任何 `@keydown`（方向鍵/Enter/Esc 全無反應）；`<input>` 沒有 `role="combobox"` / `aria-expanded`；`<ul>` 沒有 `role="listbox"`。

鍵盤或讀屏使用者**無法完成存入流程** —— 因為 `submitRows` 硬性要求 `row.selectedItem`（「請先從搜尋結果選擇物品」），而唯一能設定它的路徑就是滑鼠點擊 `<li>`。手打完整品名也不行。

另外兩個一致性瑕疵：物品下拉用 `@click`、地點下拉用 `@mousedown.prevent`；且物品下拉**沒有 blur 關閉邏輯**（地點下拉有），所以建議清單點到別處後會一直懸浮遮住下方欄位。

**修復**：抽成 `<ItemAutocomplete>` 元件（同時消掉 5 份複製），實作標準 combobox 鍵盤模式。

**工作量**：中

---

### C10. 🟡 最新的 `InventoryListView` 幾乎沒有複用既有元件

**位置**：`frontend/src/views/InventoryListView.vue`

6 個共用元件裡只用了 1 個（`SearchBox`）。其餘手寫重做：`FilterBar` → 手寫篩選卡片；`StatusBadge` → 手寫 badge；既有 5 份 modal 樣板 → 手寫第 6 份；`ConfirmModal` → 「出庫」是破壞性操作但無二次確認；共用 `flash()` → 第 5 份複製。

尤其 `:129-131` 的「物品（uuid 或完整名稱）」是**純文字框**，要管理員手打 uuid —— 而 `MyPlayerView.vue` 已經有能用的物品 autocomplete，同一個 `/item/search` API。**後台管理員的輸入體驗反而比玩家差。**（這是我上一輪寫的，該用現成元件沒用。）

`MyPlayerView.vue` 更徹底：748 行、**0 個共用元件匯入**，4 個表格、5 個 autocomplete、5 個空狀態、7 個 alert、4 個 loading 全部各寫一份。

**修復**：改用 `FilterBar` + `StatusBadge`；抽 `<BaseModal>` 收掉 6 份樣板；抽 `<ItemAutocomplete>` 供兩邊共用（後台順便得到自動完成）。

**工作量**：中

---

### C11. 🟡 科幻頁的輸入框邊框對比 1.56:1，看不出哪裡可以打字

**位置**：`frontend/src/assets/scifi-theme.css:16, 82-87`

實算（WCAG 1.4.11 非文字對比要求 3:1）：

```
--sf-border rgba(140,180,220,.22) → 疊在 #121a2b = 1.56:1  FAIL
輸入框底色  rgba(255,255,255,.03) → 疊在 #121a2b = 1.08:1  FAIL
```

輸入框底色與卡片底色差 1.08:1（幾乎相同），邊框差 1.56:1。玩家在手機戶外光線下基本看不出欄位邊界，只能靠 label 位置猜。Focus 態有明確的 accent ring 是好的，但那要先點對位置。

**修復**：`--sf-border` 提到約 `rgba(150,190,230,.45)`（≈3.1:1），輸入框底色改用與卡片有明顯落差的實色。

**工作量**：小

---

### C12. 🟡 7 分頁的資訊架構不合理，且分頁狀態不同步 URL

**位置**：`frontend/src/views/MyPlayerView.vue:391-400, 27-34, 378`

**(a) 分頁狀態遺失**：`activeTab` 是純 `ref`，沒有 URL 或 localStorage 同步。玩家在「庫存紀錄」滑到一半重新整理 → 回到「存入」。手機切到 Discord 再切回來（頁面被回收）→ 回到「存入」。瀏覽器返回鍵不會退回上一個分頁而是直接離開 `/me` —— 手機上最常見的誤觸。第 378 行匯入了 `watch` 卻整個檔案沒用到，看得出原本打算做但沒完成。

**(b) 存入/取出應該是一個畫面**：兩者是同一份 UI 的複製，唯一差異是取出多一個備註欄與 endpoint。Script 端已經正確抽出共用邏輯（`submitRows()` / `makeRow(withNote)`），**只有 template 沒抽** —— 約 70 行逐字重複。對玩家而言「記錄一筆物品變動」是一件事，方向是參數。分兩個分頁的代價：想「放 5 個進去、拿 2 個出來」必須切分頁、送兩次。

**(c) 7 個分頁層級是平的但語意不平**：存入/取出是**動作**，查詢/我的庫存/庫存紀錄/藍圖是**檢視**，我的資料是**設定**。

**建議 IA**：
```
[ 庫存 ]  ← 主畫面，右上「＋登記變動」開 sheet，內含存入⇄取出 toggle
[ 紀錄 ]
[ 藍圖 ]
[ 查詢 ]  ← 或收進「庫存」頁的搜尋框
右上 ⚙ → 我的資料 / 登出
```
並用 `router.replace({ query: { tab } })` 同步 URL。

**工作量**：中（合併存入/取出 + URL 同步）／大（完整重整）

---

### C13. ⚪ 其他 UI 項目（合併）

| 項目 | 位置 | 說明 |
|---|---|---|
| 21 個純圖示按鈕 + 7 個關閉鈕沒有可讀名稱 | 5 個列表頁 + 6 個 modal | 全庫 `aria-` 只出現 1 次。讀屏使用者無法分辨同列的鉛筆鈕與垃圾桶鈕 |
| 分頁元件沒有任何 ARIA role | `MyPlayerView.vue:27-34` | 且 7 個面板全用 `v-show`（都在 DOM 裡），讀屏線性瀏覽會一路穿過全部內容 |
| Session 過期顯示錯誤的「網路錯誤」 | `stores/playerAuth.js:140-145` | 401 且 refresh 失敗時只 `clearAuth()` 不導向，玩家看到「網路錯誤」，重試永遠不會好 |
| 找不到玩家時永遠轉圈 | `PlayerDetailView.vue:53-55` | 沒有 404 狀態，只能按瀏覽器返回 |
| 12 個空狀態沒有行動指引 | 全部列表頁 | 新玩家第一次登入看到「目前沒有登記任何物品。」，沒告訴他下一步 |
| 註冊成功後停在原地 | `RegisterView.vue:61-68` | 全欄位 disabled，唯一出口是頁面最底一行小連結，文案還是給「已有帳號」的人看的 |
| `btn-scifi` 漸層暗端對比 2.98:1 | `scifi-theme.css:116-119` | 按鈕右下約 1/3 區域文字低於 AA |
| 停用的「遊戲ID」欄位看起來可編輯 | `MyPlayerView.vue:347` | 科幻覆寫勝出 Bootstrap 的 disabled 樣式，玩家會點進去打字 |
| 手機上 7 個分頁折成 2-3 排 | `MyPlayerView.vue:27-34` | 吃掉約 100-130px 首屏，且分頁底線視覺斷裂 |
| `100vh` 在手機被工具列遮住 | `DashboardLayout.vue:117, 177` | 固定版面 + `overflow:hidden`，底部 60-90px **無法捲到**。改 `100dvh` |
| 表格列可點擊但鍵盤無法觸發 | `PlayerListView.vue:41` | `<tr @click>` 沒有 tabindex/keydown，鍵盤使用者無法進入詳細頁 |
| 沒有任何 `<h1>` | 全部 view | 標題從 h4/h5 起跳，沒有可用的文件大綱 |
| 公開站也在跑後台主題系統 | `App.vue:6-7` | 且共用 `admin_*` localStorage key（同 origin），目前無害但會撞名 |
| 死 class / 未使用匯入 / 註解過期 | `LoginView.vue:2`、`MyPlayerView.vue:2, 378` | `.login-bg` 無定義；`watch` 未使用；註解說「6 個分頁」實際 7 個 |

---

# D. 效能

> 誠實前提：這是數十人的公會工具。但 `item_master` 等主檔是**數千筆且每筆帶完整 API 原始 JSON** 的胖文件。下面的排序完全基於這個不對稱 —— **遊戲主檔的查詢會痛，公會自己的資料幾乎都不痛。**

### D1. 🔴 `ItemMaster.search()` 全表掃描，且 Discord 每按一次鍵就掃一次

**位置**：`src/models/item.py:45-48, 53-57`

三個問題疊在一起：

1. `:47` 的 `class_name` 是**不加錨點的大小寫不敏感 regex**，`src/mongo.py:31` 那個 `create_index('class_name')` 對它完全沒用。Mongo 只能撈出所有 `is_current=True` 的鍵，再 FETCH 每一筆胖文件比對。`PROJECTION` 排除 `raw` **不會**減少這個 I/O，因為 filter 必須在完整文件上求值。
2. `:55` 的 fallback 是**沒有 `^`** 的 regex，純 collection scan。觸發條件是「前綴查不到」—— 玩家打詞中間片段、或打中文（`name_zh` 根本沒被搜尋）時**每次都會走到**，等於一次請求掃兩遍。
3. `.sort('name')` 疊在 `$or` 上會變成 blocking in-memory sort。

呼叫端：`bot/completers.py:23` 的 Discord autocomplete **每一個按鍵**都送一次（沒有也不可能 debounce），打 `medical` 就是 7 次全表掃描；`/item/search` 只有 `@jwt_required()`、**沒有 rate limit**，任何登入者可無限觸發。

**修復**
1. 拿掉 `class_name` regex 分支（玩家不會打 class name）
2. fallback 換成 text index，順便把中文名補進去：
   ```python
   db['item_master'].create_index(
       [('name', 'text'), ('name_zh', 'text')],
       name='item_text', default_language='none')   # 中文要 'none'
   ```
3. `/item/search` 加 `@limiter.limit('60 per minute')`
4. Discord autocomplete 走 Redis 快取

**工作量**：中

---

### D2. 🟡 兩個 `/history` 端點的 N+1：最多 200 次往返，每次拉整包 `raw`

**位置**：`app/player/view.py:444-456`、`app/inventory/view.py:257-264`、`bot/cogs/stock.py:187-191`

那個 `names` dict **確實**避免了同一個 `item_id` 重複查，但它只去重、不批次。100 筆紀錄對應 100 個不同物品就是**扎扎實實 100 次 `find_one`**。而 `ItemMaster.get()` **沒有 projection**，每次把完整文件（含 `raw`）撈回來 —— 只為了取 `name` 和 `name_zh` 兩個字串。約 1 MB 網路傳輸換來 100 個短字串。

**修復**：改單一 `$in` 批次查詢 + projection，1 次取代 100 次，傳輸量從 ~1MB 降到 ~10KB。

**工作量**：小

---

### D3. 🟡 `ItemMaster.get()` 全專案沒有一處帶 projection

**位置**：`src/models/item.py:36`；最明顯的呼叫端 `app/item/view.py:123-128`

```python
item = ItemMaster.get(item_id)          # 從 DB 撈回整包 raw
if request.args.get('with_raw') != '1':
    item.pop('raw', None)               # ...然後在 Python 裡丟掉
```

同樣的浪費出現在每一次 `/inventory/add|remove`、`/player/inventory/add`，實際只用到 `_id` / `name` / `volume_uscu` 三個欄位。

**修復**：`get()` 加 `projection` 參數，預設 `{'raw': 0}`。

**工作量**：小

---

### D4. 🟡 `*_versions` 快照複製整包 `raw`，而且從來沒有任何程式讀它

**位置**：`tasks/scdata_sync.py:69-80`（`snapshot = dict(doc)` 含 `raw`）

每次同步為每個物品寫一筆版本快照，內容是完整文件**含 `raw`**。也就是每個遊戲版本就多存一份等同整個 `item_master` 大小的資料。SC 一年約 4 個 patch，幾年下來是 `item_master` 的 10–20 倍體積。

而 grep 全專案：**沒有任何一行讀 `*_versions`**。設計註解說是「用來比對 patch 之間的數值變動」，但那個功能還不存在。

**修復**：快照加一行 `snapshot.pop('raw', None)`。要比對數值變動只需要拉平後的欄位。這一行能砍掉未來 90% 以上的儲存成長。

**工作量**：小

---

### D5. 🟡 `/item/sync-status` 每次跑 4 個聚合查詢，而前端在同步期間每 5 秒輪詢它

**位置**：`app/item/view.py:233-245`、`frontend/src/views/SettingsView.vue:288-291`

一次請求 = `SyncRun.latest()` + 一個 `distinct` + 三個 `count_documents`。`distinct` 的 filter 是 `is_current` 但 key 是 `game_version`，沒有複合索引拿不到乾淨的 DISTINCT_SCAN。

而 `setInterval(fetchSyncStatus, 5000)` 在按下「立即同步」後啟動 —— 這 5 秒一次的掃描**正好發生在那三個 collection 被 bulk_write 猛寫的 5–10 分鐘裡**，約 60–120 次。最需要靜下來的時候偏偏最吵。（這是我上一輪寫的。）

**修復**：Redis 快取整個 payload 30–60 秒；補 `(is_current, game_version)` 複合索引；輪詢間隔從 5 秒放寬到 15 秒 —— 5–10 分鐘的作業不需要 5 秒精度。

**工作量**：小

---

### D6. ⚪ 其他效能項目（合併）

| 項目 | 位置 | 說明 |
|---|---|---|
| 12 個宣告了但沒有任何查詢用到的索引 | `src/mongo.py` | 實際寫入成本接近零（Mongo 只重寫鍵值真的變動的索引），值得刪的理由是**消除「搜尋有索引」的錯覺**，尤其 `class_name` 那三個 |
| `sync_runs` 用 `finished_at` 排序但索引建在 `started_at` | `tasks/scdata_sync.py:224` | COLLSCAN，但一週一筆所以無感。心跳改用 `started_at` 即可 |
| `inventory_log` 索引前綴對不上查詢 | `src/mongo.py:58` | 目前規模無感。順帶：這個 collection **沒有任何 TTL 或清理任務**（`logs` 有），是成長最快的 |
| `PAGE_SIZE_VEHICLES = 5` | `src/__init__.py:70` | items/commodities 是 100/頁，vehicles 卻是 5 → 約 50 次 HTTP 往返，是「130 次外部請求」裡最大的一塊。改 config 就好 |
| 前端整套 bootstrap-icons 字型 | `frontend/src/main.js:6-8` | woff2 130KB + woff 180KB + ~100KB CSS，實際用到約 20 個圖示。gzip 後總量 60–80KB，對公會工具不痛，唯一有感的是那個 180KB woff |
| `list_stock` + `capacity` 一次請求三趟 DB | `src/models/inventory.py:112, 170-185` | `$match` 在 `$lookup` 前是**正確的**；`$limit` 無法前推是因為排序欄位來自 join，設計必然。可省的是 `capacity()` 完全沒有 limit。庫存破千列再處理 |
| `InventoryListView` 只搜已載入的 200 筆 | `InventoryListView.vue:194-201` | 庫存超過 200 筆時**使用者搜不到的東西其實存在** —— 這比慢更糟。至少加「僅顯示前 200 筆」提示 |

---

# E. 程式碼品質

### E1. 🟡 後端 5 處逐字重複的 helper，且已開始行為漂移

**位置**：`app/player/view.py:26-29, 285-292, 295-305, 435-439, 443-456` ↔ `app/inventory/view.py:30-33, 67-76, 79-89, 36-45, 256-263`

量化：`_positive_int` 逐字相同（11 行）；`_resolve_item_or_400` 僅錯誤訊息差一個詞（8 行）；`StockError` handler 相同（4 行）；limit 解析是 `_paging()` 的退化複本（5 行）；補物品名稱的迴圈同一 pattern，但 player 版多帶 `name_zh` —— **兩邊回傳的 history 形狀已經開始分歧**。合計約 40 行。

**修復**：新增 `app/_shared.py`（或 `src/api_helpers.py` 讓 bot 也能共用），集中 `paging()` / `positive_int()` / `resolve_item_or_400()` / `attach_item_names(rows)`。`attach_item_names` 統一帶 `name` 與 `name_zh`（前端多一個欄位無害，少一個會壞）。同時把 `_serialize_form` 改成由欄位清單產生的 factory，避免 B1 的 bug 在第三個模組再長一次。

**工作量**：小

---

### E2. 🟡 前端重複樣板

**位置**：`flash()` × 5（其中 1 份簽章不同）、`escHtml()` × 4、`.alert-slide` CSS × 14、`new Modal()` × 6、`fmtDate()` × 5（行為不一致）、`ROLE_LABELS` × 2

具體後果：14 份 alert-slide 裡只有 4 份定義了 leave transition，所以同一個產品裡提示訊息**有時淡出、有時瞬間消失**。5 份 `fmtDate` 讓同一個 `created_at` 在玩家列表顯示「2026/8/21」、使用者管理顯示「2026/8/21 下午2:03:22」、庫存紀錄顯示「2026/8/21 14:03:22」。連刪節號字元都不一樣（`載入中...` vs `載入中…`）。

**修復**：`utils/format.js`（`fmtDate` / `fmtDateTime` / `escHtml`）+ `composables/useFlash.js` + 一個全域 `.alert-slide`。

**工作量**：小

---

### E3. 🟡 稽核日誌只覆蓋 5 處

**位置**：全庫 `Log.create` 只有 `app/auth/view.py:58, 71` 與 `app/inventory/view.py:312, 368, 427`

完全沒有稽核紀錄的包括：使用者的建立／修改／刪除（**最敏感的操作**）、玩家 CRUD 與軟刪除、藍圖全部寫入、`/item/sync` 觸發與排程修改、device 相關。`CLAUDE.md` 的範本明確把 `Log.create(...)` 列為寫入路由的標準組成。

意味著後台 `/logs` 頁面呈現的是嚴重不完整的操作史，出事無法回溯。

**修復**：既然 E1 要抽共用模組，在那裡加一個 `@audited(action)` 裝飾器，一次補齊所有模組。

**工作量**：小

---

### E4. 🟡 `vite.config.js`（後台 build）缺 `/item` 與 `/inventory` proxy — 同一個 bug 又發生一次

**位置**：`frontend/vite.config.js:30-40` ↔ `frontend/vite.config.web.js:38-49`

兩份 config 的 proxy 清單不對稱：web build 有 `/item` 與 `/inventory`，admin build **沒有**。但 `InventoryListView.vue` 與 `SettingsView.vue` 的整個同步／排程區塊都是**後台專屬頁面**，正是掛在這個 build 上。所以 `npm run dev` 時這兩個最新功能完全連不到 Flask（生產環境因為同源走 nginx 所以沒事）。

這就是之前咬過一次的那個類別 —— 硬編碼的路徑清單漂移。另外 `vite.config.js` 還留著 `/sample` 與已死的 `/loot`。

**修復**：把 proxy 清單抽成共用陣列（`vite.proxy.mjs` 導出 `makeProxy(paths)`），兩份 config 都 import，從此不可能再漂移。

**工作量**：小

---

### E5. 🟡 nginx 反向代理是硬編碼的藍圖清單 — 目前漏 3 個、多 1 個死條目

**位置**：`conf/nginx-web/default.conf:26` ↔ `app/__init__.py:91-101`

| | 內容 |
|---|---|
| 已註冊的 blueprint | `sample, auth, user, admin, log, device, docs, item, inventory, player, blueprint` |
| nginx 正則 | `auth\|user\|log\|device\|player\|blueprint\|loot\|item\|inventory` |
| **漏掉** | `/sample`、`/admin`、`/docs`，外加 flasgger 的 `/apidocs`、`/apispec_1.json`、`/flasgger_static`（全部落到 SPA fallback，回 HTML 而非 JSON） |
| **多餘** | `loot` —— 後端從未註冊（見 C6） |

另一個安全面向：公開玩家站的正則把 `/user`、`/log`、`/inventory`、`/player`、`/blueprint` **整套後台 API 都代理出去了**。配合 A3，公開站就是完整的後台 API 端點。

**修復**：改成「**反向白名單**」—— 只把 `/assets`、`/nginx-health` 之類排除掉，其餘全部 `proxy_pass`。這樣新增 blueprint 不需要改 nginx，永久消滅這類 bug。若要維持列舉式，至少在 `app/__init__.py` 加一個列出所有 `url_prefix` 的常數，並寫測試比對 nginx conf。

**工作量**：小（補漏）／中（改架構 + 加測試）

---

### E6. 🟡 兩個檔案過大且職責混雜

**位置**：`frontend/src/views/MyPlayerView.vue`（748 行 / 7 分頁）、`app/player/view.py`（553 行 / 4 種職責）

`app/player/view.py` 把四件不相干的事放一起：後台 CRUD（69-132）、公開註冊／登入／refresh（139-216）、玩家自助個人庫（281-457）、玩家自助藍圖（466-553）—— **這正是 A3、B1 兩個 bug 能同時存在於一個檔案的原因**：後台區塊沒有 role 檢查，自助區塊卻做了嚴謹的 `player_required`，讀的人很容易誤以為整檔都受保護。

**修復**：`MyPlayerView.vue` 的 7 個分頁只共用 4 個 helper，耦合很低 → 拆成 `views/me/` 下的子元件 + `composables/usePlayerApi.js`，幾乎沒有風險。`app/player/view.py` 拆成 `app/player/view.py`（後台 CRUD）與 `app/me/view.py`（自助），這樣「哪些路由需要 role」變成**目錄層級的事實而非逐行慣例** —— 建議與 A3 一起做。

**工作量**：中

---

### E7. ⚪ 中文地點對照表前後端各存一份，且後端的查詢函式是死碼

**位置**：`src/data/sc_location_names_zh.json` ↔ `frontend/src/assets/sc-locations-zh.json`（md5 相同）、`src/sc_zh.py:41-46`

兩份 JSON 位元相同（38 個 key），遊戲改版後必須手動同步兩邊。而 `src/sc_zh.py` 的 `location_name_zh()` **從未被任何地方呼叫** —— 前端選擇複製 JSON 並用 JS 重新實作查表。（這是我上一輪的疏漏。）

**修復**：刪掉前端那份，在 `/inventory/locations` 回傳時就用 `location_name_zh()` 補上 `name_zh`（前端已有處理 `item_name_zh` 的慣例）。

**工作量**：小

---

### E8. ⚪ 其他程式碼品質項目（合併）

| 項目 | 位置 | 說明 |
|---|---|---|
| API 回應格式三種寫法並存 | `app/auth/view.py` 不用 `data` 包裝；其餘用 `data` 但又平鋪 9 種 ad-hoc 頂層 key；`/` 回裸字串 | 建議 `src/responses.py` 的 `ok()` / `fail()` |
| 同一種失敗兩個狀態碼 | 「找不到物品」在 `inventory:165`/`item:125` 是 404，在 `inventory:75` 走 `StockError` 是 400 | 統一成 404 |
| 「移庫」功能約 140 行沒有任何使用者能觸發 | `app/inventory/view.py:380-442` + `src/models/inventory.py:257-333` | 後端有完整的補償式交易 + rollback 稽核，前端 `inventoryApi.move` 無呼叫者，bot 也沒用。建議**補 UI** 而不是刪 |
| 註解指向已不存在的東西 | `app/item/view.py:254-255` 寫「每週排程（scdata-sync-weekly）」 | 那個 beat 條目已在改 DB 排程時移除，現在叫 `check-sync-schedule` |
| 只有 2/9 個 blueprint 目錄有 `__init__.py` | `app/blueprint/`、`app/player/` 有，其餘 7 個沒有 | 靠 PEP 420 目前能跑，但打包或加 `conftest.py` 時會出現難診斷的差異 |
| `conf/config.ini` 沒進 `.gitignore` 也沒進 `.dockerignore` | `.gitignore:67` 只忽略 `conf/flask.json` | 目前內容是範本值，但輪替密碼後就會把真密碼提交進 git 並烘進 image |
| `TestingConfig` 命名誤導 | `run.py:34` | **查證無害**（見附錄），但任何人看到都會警戒，且有人「順手」把 `TESTING` 改 `True` 就真的會噴 traceback。改用 `ProductionConfig` |
| `Inventory.move` 把 pymongo 原始例外回給前端 | `src/models/inventory.py:321-324` | `DuplicateKeyError` 訊息會帶出資料庫名、索引名與另一位玩家的 handle。只有 admin/operator 能觸發，機率低 |
| `/device/unregister` 沒有歸屬檢查 | `app/device/view.py:55-61` | 知道別人的推播 token 就能刪掉它。token 是長隨機字串不會外流，實務上幾乎不可利用 |
| `star_citizen_id` 完全沒有格式驗證 | `app/player/view.py:145` | 可以是任意字元任意長度。而這個值會成為 JWT identity 的一部分（`player:<scid>`）—— 用字串前綴區分兩套身分體系本身就脆弱。建議加 `[A-Za-z0-9_-]{3,60}` 白名單 |
| 缺 CSP，`X-XSS-Protection` 已廢棄，HSTS 被註解掉 | `app/__init__.py:73-79`、`conf/nginx/...cloudflare.template:44` | 已設 `X-Frame-Options`/`X-Content-Type-Options`/`Referrer-Policy`（都好） |

---

# F. 測試

### F1. 🟡 近期新增的功能零測試覆蓋

專案**有**一套 pytest（`tests/` 11 檔 1492 行、`pytest.ini`、`conftest.py`），品質不錯 —— `test_inventory.py` 有 30 個模型層 case，`test_inventory_api.py` 有 `test_write_requires_operator_role`、`test_item_search_escapes_regex`。

但 grep 全 `tests/` 無任何引用的模組：

- `src/models/player.py`
- `src/models/blueprint.py`
- `src/models/sync_schedule.py`
- `app/player/view.py` —— **含玩家註冊／登入／refresh／自助庫存／自助藍圖全部 17 支路由**
- `app/blueprint/view.py`（5 支）
- `app/item/view.py` 的 `/sync`、`/sync-status`、`/sync-schedule`（3 支）

也就是說**這幾輪對話加的所有東西都沒有測試**。

**關鍵缺口**：`test_inventory_api.py` 有測「viewer 不能寫 `/inventory/add`」，但**沒有任何測試檢查 viewer 或玩家 token 打 `/player/`、`/blueprint/`、`/log/` 會不會被擋** —— 所以 A3 這種等級的洞完全沒有測試網。

我沒有找到會因簽章或路由變更而 fail 的既有測試（現有測試對到的 API 都還在），問題純粹是「新東西一個都沒測」。

**修復（建議優先做第 1 項）**
1. 新增 `tests/test_permissions.py`，參數化把「**每一支後台路由 × {未登入, viewer token, 玩家 token}**」跑一遍斷言 401/403 —— 這一支測試同時是 A3、A4 的回歸保護，而且能防止未來新增路由忘記加裝飾器
2. 補 `tests/test_player_api.py`、`tests/test_blueprint_api.py`、`tests/test_sync_schedule.py`
3. 把 pytest 放進 CI

**工作量**：中

---

# 附錄：已查證為「非問題」的項目

這些在初步掃描時被標為疑慮，但深入驗證後**不成立**。列出來是為了讓你不必浪費時間，也不必擔心。

### 因暫存檔案不完整造成的假警報（你的專案沒有這些問題）

| 疑慮 | 實際情況 |
|---|---|
| `src/sample.py` 不存在 → `create_app()` 會 ImportError、gunicorn 起不來、整套 pytest 無法 collect | **檔案存在**（348 bytes）。我第一批只暫存了 134 個程式檔沒包含它 |
| `app/templates/` 不存在 → `GET /docs/` 必定 500 | **存在**（`app/templates/docs/index.html`，46KB）。我刻意排除了 build 產物目錄 |
| `env.new` 是 `.env` 的複本且未被 `.dockerignore` 排除 → 機密被打進 image | **你的 Mac 上沒有這個檔案**。它只存在於我的稽核容器暫存目錄（是先前 session 的殘留） |
| 專案根目錄沒有 `.gitignore` | **有**（2064 bytes） |
| `docker-compose.no-nginx.yml.sample` 不存在 → README 的部署步驟會失敗 | **存在**（1505 bytes） |
| `frontend/package-lock.json` 不存在 → `npm ci` 會失敗 | **存在**（47KB） |

### 深入驗證後不成立的技術疑慮

| 疑慮 | 驗證結果 |
|---|---|
| `run.py:34` 用 `TestingConfig` 上線很危險 | **無害**。`conf/config.py:29-33` 的 `TestingConfig` 只設 `TESTING=False`、`DEBUG=False`（兩個都明確關閉）、`DB_SERVER`、`DATABASE_URI`（後兩個全專案**沒有任何地方讀取**）。等同 `BasicConfig`，只是命名誤導 |
| 500 會噴 stack trace 給客戶端 | **不會**。`app/__init__.py:82-87` 一律回「伺服器內部錯誤」，`DEBUG`/`TESTING` 皆 False 且未設 `PROPAGATE_EXCEPTIONS` |
| `ensure_indexes()` 從未被呼叫，資料庫可能一個索引都沒有 | **有呼叫**。`run.py:35`。（這原本是效能審查裡最高優先的未知項，現已排除） |
| MongoDB operator injection（`$ne` / `$gt` 注入） | **無法注入**。所有把 `request.json` 送進 filter 的路由都經過 `.strip()`，傳 dict 只會 `AttributeError` |
| `src/models/item.py` 的 regex escape 不一致 → ReDoS | **一致**。`escape_regex()` 在三個分支全都套用，`re.escape` 讓 metacharacter 無法存活。`tests/test_inventory_api.py:248` 也有對應測試 |
| `ConfirmModal` 的 `v-html` 造成儲存型 XSS | **目前沒有**。5 個呼叫點全都套 `escHtml`，插入位置是 element text content。但架構是「元件用 `v-html`，靠呼叫端自律」，第 6 個呼叫點忘記就會出事 —— 列為 E8 的脆弱設計提醒 |
| `sync_runs` collection 與 `ok`/`finished_at` 欄位名稱是上一輪推測的，可能對不上 | **推測正確**。`tasks/scdata_sync.py:186-197` 確實寫入 `sync_runs` 含 `ok` 與 `finished_at`；`src/models/item.py:198` 的 `SyncRun.COLLECTION = 'sync_runs'` 一致；`src/mongo.py:48` 有對應索引 |
| `InventoryListView.vue`（新增）與後端請求／回應形狀不符 | **完全吻合**。`list_stock` 的 9 個欄位與 `capacity` 的 4 個欄位逐一對得上 |
| `tasks/scdata_sync.py` 是否逐筆 upsert（數千次往返） | **沒有**。正確使用 `UpdateOne` + `bulk_write(ordered=False)`，`BULK_SIZE=500` 分批 |
| `src/sc_zh.py` 的 256KB 中文對照表每次呼叫重讀 | **快取正確**。module-level global，只在第一次呼叫時載入。而且只被 `src/scdata.py` 用到，所以只有 worker 在同步時才載入，Flask 進程根本不會碰 |
| `MyPlayerView.vue` 的搜尋每按鍵打一次 API | **已 debounce 300ms** |
| 路由層沒有 code splitting | **正常運作**。每個 route 都是動態 `import()`，兩個 build 共用同一份 router，Vite 自動切 chunk |
| 科幻主題漏進後台管理頁 | **沒有漏**。12 組選擇器全部已用 `.scifi-page` 前綴限定，`--sf-*` 變數與後台的 `--sb-*`/`--nb-*` 完全不相交。真正的洩漏是反方向的（見 C0） |
| 後台列表頁刪除沒有二次確認 | **有**。`PlayerListView` / `BlueprintListView` / `LootListView` 全部用 `ConfirmModal`。真正的例外只有玩家端（C7） |
| `list_stock()` 的 `$match` 在 `$lookup` 之後（效率問題） | **在之前，是正確的**。且走得到 `inv_unique` 索引前綴，`$lookup` 打的是主鍵。`$limit` 無法前推是因為排序欄位來自 join，設計必然 |
| `MongoClient` 每次請求建連線 | **沒有**。module-level singleton，連線池正常複用 |
| `BlueprintListView` / `UsersView` 的關聯顯示是 N+1 | **不是**。先把清單抓進 store 再做前端 join，沒有 per-row API 呼叫 |
| `sync_schedule` collection 缺索引 | **不需要**。單一文件 `_id='default'`，內建 `_id` 索引就夠 |

---

# 建議執行順序

| 階段 | 項目 | 預估 | 為什麼是這個順序 |
|---|---|---|---|
| **1** | A1 改 admin 密碼與 `.env` 四個密碼 | 5 分鐘 | 未登入者一步取得 admin |
| **2** | A2 三個 DB port 改綁 `127.0.0.1` | 5 分鐘 | 未登入者可直連無認證的 Mongo |
| **3** | A3 + B1 + A4 補角色控管與修 `_serialize_form` | 半天 | 同一批檔案，權限與資料破壞雙重風險。**做完立刻寫 F1 的第 1 項測試把它鎖住** |
| **4** | B2 + B3 + B5 同步加 Redis 鎖、修 backoff、抽 `_do_sync` | 半天 | 同一段程式，避免物品主檔被誤標下架 |
| **5** | B4 cron 時區 + E4 vite proxy | 20 分鐘 | 各 10 分鐘，開發體驗與排程正確性立刻改善 |
| **6** | C0 設計 token 層（一次修掉 C1/C4/C11 + 2 個輕微項） | 1–1.5 天 | 前端投報率最高的一步，且讓後台深色模式第一次真的能用 |
| **7** | C2 focus ring + C3 手機表格 + C7 刪除確認 | 半天 | 三個各半小時到一小時的獨立小修 |
| **8** | C6 清掉 loot 死碼 + C5 統一錯誤層 | 1 天 | 一次性大清掃（約 450 行），順帶讓錯誤不再被渲染成空狀態 |
| **9** | D1 + D2 + D3 + D4 主檔查詢與 N+1 | 1 天 | D1 是唯一會隨資料成長而惡化的地方 |
| **10** | E1 + E2 + E3 抽共用模組與 `@audited` | 1 天 | 這三個共用同一批重構動作，一起做最省 |
| **11** | E6 拆檔 + C8/C9/C10 元件化 + C12 IA 重整 | 2–3 天 | 需要產品決策，不急 |
| 其餘 | A5–A8、B6、B7、C13、D6、E5、E7、E8、F1 剩餘 | 依節奏 | |

**如果只想做一件事**：做階段 1 + 2（10 分鐘，改設定不改程式）。
**如果想做一個下午**：加階段 3（權限 + 資料破壞），這是三個真實缺陷裡最危險的。
